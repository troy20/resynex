import Link from "next/link";
import { prisma } from "@/lib/prisma";
export default async function Facilities(){const facilities=await prisma.facilityListing.findMany({include:{user:true},orderBy:{createdAt:'desc'}});return <main className="mx-auto max-w-7xl px-6 py-10"><h1 className="text-3xl font-bold">University Facilities</h1><p className="mt-2 text-slate-600">Browse labs, instruments, and university R&D services.</p><div className="mt-8 grid gap-5 md:grid-cols-2">{facilities.map(f=><Link href={`/facilities/${f.id}`} key={f.id} className="rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200 hover:shadow-lg"><div className="text-lg font-semibold">{f.title}</div><div className="mt-2 text-sm text-slate-500">{f.institutionName} · {f.location}</div><p className="mt-3 line-clamp-3 text-slate-600">{f.description}</p></Link>)}</div></main>}
