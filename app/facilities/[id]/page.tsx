import { prisma } from "@/lib/prisma";
import { notFound } from "next/navigation";
export default async function FacilityDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const f=await prisma.facilityListing.findUnique({where:{id},include:{user:true}});if(!f)notFound();return <main className="mx-auto max-w-4xl px-6 py-10"><div className="rounded-3xl bg-white p-8 shadow-sm ring-1 ring-slate-200"><h1 className="text-3xl font-bold">{f.title}</h1><div className="mt-3 text-slate-500">{f.institutionName} · {f.location} · {f.facilityType}</div><p className="mt-6 whitespace-pre-wrap leading-8 text-slate-700">{f.description}</p><div className="mt-6"><b>Equipment:</b> {f.equipmentList.join(', ')}</div><div className="mt-8 flex gap-3"><a href="/#auth" className="rounded-xl bg-blue-600 px-5 py-3 font-semibold text-white">Request booking</a><button className="rounded-xl border px-5 py-3 font-semibold">Save</button></div></div></main>}
